---
description: Git commit
auto_execution_mode: 1
---

Git commit, following the git commit standards in my git.md rules. No commit message
scopes. No lengthy body messages. No Co-Authored-By statements. Use the most appropriate
of all of the supported commit types listed in those standards.

If this project is a library, only use `fix` for changes that are relevant to consumers,
not development tooling fixes, etc.
